import styled from "@emotion/styled"

export const ShowPassowrd = styled.div`
  position: relative;

  cursor: pointer;

  svg {
    position: absolute;
    right: calc(0px + 10px);
    bottom: 12px;
  }
`

export const ForgotPassword = styled.div`
  position: relative;

  p {
    position: absolute;
    right: calc(0px + 10px);
    top: 5px;
    z-index: 1;
  }
`
