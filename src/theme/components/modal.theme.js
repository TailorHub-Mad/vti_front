export const Modal = {
  baseStyle: {
    overlay: {
      bgColor: "rgba(5, 46, 87, 0.8)",
      pointerEvents: "none"
    },
    dialog: {
      pointerEvents: "auto"
    },
    dialogContainer: {
      pointerEvents: "none"
    }
  }
}
