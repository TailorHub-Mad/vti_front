export const Modal = {
  baseStyle: {
    overlay: {
      bgColor: "rgba(5, 46, 87, 0.8)",
      position: "fixed",
      zIndex: "10000"
    },
    dialog: {
      pointerEvents: "auto",
      position: "fixed"
    },
    dialogContainer: {
      pointerEvents: "none",
      position: "fixed",
      zIndex: "10001"
    }
  }
}
